

ServerConfig:
    parse the config file, prepare serverBlocks(with locations inside)


ServerBlocks:
    the individual server {}:
        port,
        domainName,
        Locations ,
        root etc